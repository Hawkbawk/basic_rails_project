<p>
    <strong>Title:</strong>
    <%= @article.title %>
</p>

<p>
    <strong>Text:</strong>
    <%= @article.text %>
</p>

<h2>Comments </h2>
<% if @article.comments.count == 0 %>
    <b>No comments yet!</b>
<% end %>
<ul>
<%= render @article.comments %>
</ul>

<h2> Add a comment: </h2>
<%= render 'comments/new_comment'%>

<%= link_to 'Create Another', new_article_path %>
<%= link_to 'Edit', edit_article_path(@article) %>
<%= link_to 'Back', articles_path %>